<h1>PackExport list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Pack_id</th>
            <th>Language_id</th>
            <th>Filepath</th>
            <th>Created</th>
            <th>Created_by</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('export_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.packid }}</td>
            <td>{{ entity.languageid }}</td>
            <td>{{ entity.filepath }}</td>
            <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{{ entity.createdby }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('export_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('export_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('export_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
