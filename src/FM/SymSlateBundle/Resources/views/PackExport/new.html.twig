{% extends '::base.html.twig' %}

{%  block body %}

<h1>PackExport creation</h1>

<form action="{{ path('export_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
    <!--input type="hidden" name="XDEBUG_PROFILE" value="true"></input-->
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('export') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}